// Flujo de un programa: EL flujo de un programa siempre sera de arriba/abajo

// Estructuras de control de flujo

// Condicionles:
// Simples -> if 
// Compuestros -> if - else
// Multiples -> if - else y else if

// Ejemplo simple del funcionamiento del if
// if(condiciion(codigo a ejecutar)){
    // codigo a ejetucar
// }

// if() codigo a ejecutar
// else codigo a ejecutar en caso contrario

// Bucles
// Determinados (for, etc)
// Indeterminados (While, Do While, etc)

// Bucles While y Do While

// Bucle While: Es un bucle indeterminado ya que no sabemos cuantas veces dara durante su ejecucion

// Ejemplo de uso
// let pass = ''
// while(pass!='hola'){
//  pass = prompt("Introduzca su contraseña")
// }console.log('Fin del programa')

// Bucle Do While: Este bucle tambien es indeterminado ya que no se sabe cuantas vueltas dara durante su ejecucion

// Bucle for: Este bucle es determinado ya que hay que especificar cuantas vueltas dara en su ejecucion (durante su ejecucion la variable i aumentara de valor. i es un iterador) 

// Bucles for in / for of: Simplifica el bucle for tradicional sin tener que darle el numero de vueltas ni de realizar un incremento. 

// Operador ternario: Se utiliza cuando una condicion sera true o false, al igual que el if. Su ejecuion puede tener una o varias sentencias, en este caso iran separados por comas y entre parentesis

// (condicion)?True:False -> Sintaxis

// Ejemplo de uso
//  let num = 3
// (num%2===0)?console.log(`${num} es par`):console.log(`${num} es impar
