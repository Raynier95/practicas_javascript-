// JS es un lenguaje de programacion que puede interpretar el navegador

// Sintaxis
// JS es case sensitive Numero no es igual a numero
// Es de tipado dinamico o debil, las variables son del tipo del dato que almacenan
// Las sentencias finalizan con ; (no es obligatorio, pero es recomendable)

// Variables
// Una variable es un espacio que reservamos en memoria para almacenar un dato que podra cambiar durante la ejecucion del programa. 
// La palabra reservado para emplear es "let" (No es recomendable usar var)

// let auto = 'toyota' //Ejemplo de variable en JS

// Una varaible se puede declarar, inicializar y modificar

// Constantes
// Una constante es un espacio que reservamos en memoria para almacenar un dato que no cambiara durante la ejecucion del programa.
// La palabra reservado es "const"

// const nombre = 'Juan' //Ejemplo de constante en JS

// El Scope o ambito es la zona donde existe nuestra variable o constante